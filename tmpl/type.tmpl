<?js
    var data = obj;
    var self = this;
    data.forEach(function(name, i) { var name2 = name.toUpperCase() ?>
<span class="param-type"><?js= self.linkto(name2, self.htmlsafe(name)) ?></span>
<?js if (i < data.length-1) { ?>|<?js } ?>
<?js }); ?>
